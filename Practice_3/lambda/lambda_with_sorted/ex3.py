products = [("Кофе", 300), ("Чай", 150), ("Сок", 200)]

# x[1] означает, что мы берем второй элемент (цену) для сравнения
cheapest = sorted(products, key=lambda x: x[1])

print(cheapest)  # [('Чай', 150), ('Сок', 200), ('Кофе', 300)]