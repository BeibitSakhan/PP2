import math
import random

# 1
area = math.pi * (5 ** 2)

# 2
val = math.sqrt(math.pow(2, 4)) # Square root of 16

# 3
colors = ['red', 'blue', 'green']
pick = random.choice(colors)

# 4
dice_roll = random.randint(1, 6)

# 5
deck = [1, 2, 3, 4, 5]
random.shuffle(deck)